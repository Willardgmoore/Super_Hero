<%= form_for @ability do |x| %>
  <%= x.label :name %>: 
  <%= x.text_field :name %>

  <%= x.label :super_with_ability_id %>: 
  <%= x.collection_select(:name, SuperPerson.all, :id, :name) %>

  <%= x.label :description %>
  <%= x.text_field :description %>


  <%= x.submit %>
<% end %>
